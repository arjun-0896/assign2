<h1>Editing Details</h1>

<%= render 'form_edit' %>


<% if session[:state] == 'admin' %>

  <div class="btn btn-success topcorner2">
    <%= link_to 'Back', '/customers' , :style =>'color:#FFFFFF;' %>
  </div>

<%else%>

  <div class="btn btn-success topcorner2">
    <%= link_to 'Back', customer_path(session[:member_id]) , :style =>'color:#FFFFFF;' %>
  </div>

<% end %>
